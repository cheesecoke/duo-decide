# Plan: Enhance the formatVoteCount utility (lib/formatVoteCount.ts)

## Requirement

<!-- selected: plan-a §2 [plan-a:requirement] -->
Enhance the formatVoteCount utility (`lib/formatVoteCount.ts`) to abbreviate large vote counts: for counts >= 1000, render a rounded thousands value with a `k` suffix (`1000 -> '1k votes'`, `1500 -> '1.5k votes'`, `12300 -> '12.3k votes'`, `1000000 -> '1000k votes'`); keep the existing singular/plural behavior for counts below 1000 (`'0 votes'`, `'1 vote'`, `'2 votes'`). Use at most one decimal place and drop a trailing `.0`. Update the existing test `__tests__/lib/formatVoteCount.test.ts` with cases covering the new thousands behavior.

## Proposed approach

<!-- merged: plan-a §3 [plan-a:proposed-approach] + plan-a §3 [plan-a:proposed-approach] -->
- [ ] Add a branch in `formatVoteCount` for `count >= 1000`: compute `k = Math.round(count / 100) / 10`, then convert to string — JS coerces `2.0` to `"2"` naturally, so no manual `.0` stripping is required.
- [ ] Append `"k votes"` to the k-value string (e.g. `"1k votes"`, `"1.5k votes"`).
- [ ] Leave the existing `< 1000` path (singular/plural) entirely unchanged.
- [ ] Floating-point and negative input validation is explicitly out of scope — the existing JSDoc declares a non-negative integer caller contract; runtime guards would be defensive code for a caller-owned invariant.

## Verification

<!-- merged: plan-a §4 [plan-a:verification] + plan-a §4 [plan-a:verification] -->
- [ ] Run `__tests__/lib/formatVoteCount.test.ts` — all pre-existing cases must still pass.
- [ ] Add new cases to the **existing** `describe` block only; no sub-describes, no helpers, no restructuring.
- [ ] Required boundary and spec cases (see Pinned test cases below).
- [ ] `npm test` (or equivalent) exits green with no new failures.

## Pinned test cases

<!-- new: exact required test cases per spec and boundary analysis [bridge:pinned-test-cases] -->
| Input | Expected output | Purpose |
|-------|----------------|---------|
| `999` | `"999 votes"` | Boundary — stays in plural path |
| `1000` | `"1k votes"` | Boundary — first k-value |
| `1500` | `"1.5k votes"` | Decimal retained |
| `2000` | `"2k votes"` | Trailing `.0` dropped by JS coercion |
| `12300` | `"12.3k votes"` | Spec example |
| `1000000` | `"1000k votes"` | No million suffix (intentional — see below) |

<!-- new: inline comment requirement to prevent future refactor regression [bridge:intentional-no-million-suffix] -->
The `1000000` test case must carry an inline comment: `// intentional: no million suffix per spec` — this pins the behavior and prevents a future refactor from silently "correcting" it to `"1m votes"`.

---

*Generated by plan-synthesizer from 1 input plan(s). See plan-map.json for source structure.*